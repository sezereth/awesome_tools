from setuptools import setup, find_packages

setup(
    name='awesome-tools',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your project dependencies here
    ],
)
